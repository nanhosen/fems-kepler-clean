import styled, { css } from "styled-components";

const ChartSpot = () => {
	return <SideDiv> Hi </SideDiv>
}

const SideDiv = styled.section`
	color:white;
	background:black;
	position: relative;
`

export default ChartSpot